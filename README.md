Eleanor ProcessWire template file
=================================

For test usage remove PHP Template tgas before picture paths and run `grunt server`.
And please run `npm install` and `bower install` before first usage.